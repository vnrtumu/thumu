import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const CreatePost = ({navigation}) => {
  return (
    <View>
      <Text>This is create a post screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default CreatePost;
